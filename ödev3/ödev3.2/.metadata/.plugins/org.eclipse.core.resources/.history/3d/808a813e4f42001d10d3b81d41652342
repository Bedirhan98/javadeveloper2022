package polymorphismDemo;

public class CustomerManager {
	BaseLogger[] loggers= new BaseLogger[] {new FileLogger(),new EmailLogger(),new DatabaseLogger()};
	for (BaseLogger b:loggers) {
	b.Log("Log message");	
	}
}
} 
